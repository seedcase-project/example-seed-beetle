name: Check Data Package standard version

on:
  schedule:
    # Runs every two weeks (every other Monday)
    - cron: 0 0 * * 1
  workflow_dispatch:

jobs:
  check-latest-version:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: install dependencies
        run: uv 
